VectorImposterFinder will inspect whether your image set has a raster image inside .svg or .pdf.

# Installation

```
bundle install
```

# Usage

```
ruby find_vector_imposter.rb [folder_path]
```

# TODO
[ ] Use regular expressions to search for raster in pdf and svg files instead of specific libraries. This should increase the speed of the utility several times. 
